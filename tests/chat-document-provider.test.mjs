import test from "node:test";
import assert from "node:assert/strict";
import { estimatePdfTokens, pdfContext } from "../lib/chat-document.ts";
import { parsePdfWithOpenRouter } from "../app/providers/openrouter/openrouter-document-adapter.ts";
import { DOCUMENT_INGESTION_STAGES, DocumentIngestionTiming } from "../app/server/chat/document-ingestion-timing.ts";
test("PDF token estimate is conservative and small PDFs inline every page",()=>{ assert.equal(estimatePdfTokens("12345"),2); const doc={id:"p",name:"a.pdf",contentType:"application/pdf",size:10,pageCount:2,tokenEstimate:2}; assert.equal(pdfContext(doc,[{pageNumber:1,text:"one"},{pageNumber:2,text:"two"}]).includes("[PDF page 2]\ntwo"),true); });
test("large PDFs contain metadata and no partial text",()=>{ const doc={id:"p",name:"large.pdf",contentType:"application/pdf",size:10,pageCount:50,tokenEstimate:40000}; const value=pdfContext(doc,[{pageNumber:1,text:"SECRET"}]); assert.match(value,/id=p.*pages=50.*40000/s); assert.doesNotMatch(value,/SECRET/); });
test("external PDF parsing records provider duration without recording request contents",async()=>{const originalFetch=globalThis.fetch;const originalKey=process.env.OPENROUTER_API_KEY;process.env.OPENROUTER_API_KEY="secret-provider-key";globalThis.fetch=async()=>new Response(JSON.stringify({choices:[{message:{content:JSON.stringify({pages:[{pageNumber:1,text:"page text"}]})}}]}),{status:200,headers:{"content-type":"application/json"}});try{const timing=new DocumentIngestionTiming({documentType:"application/pdf",byteSize:5});const pages=await parsePdfWithOpenRouter(new Uint8Array([37,80,68,70,45]),"document.pdf",undefined,timing);assert.equal(pages.length,1);assert.equal(timing.snapshot().stageDurations[DOCUMENT_INGESTION_STAGES.EXTERNAL_PARSING]!==undefined,true);assert.doesNotMatch(JSON.stringify(timing.toLogEntry()),/secret-provider-key|page text|document\.pdf/);}finally{globalThis.fetch=originalFetch;if(originalKey===undefined)delete process.env.OPENROUTER_API_KEY;else process.env.OPENROUTER_API_KEY=originalKey;}});
