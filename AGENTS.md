# Agent Instructions

## UI subagent usage

Use the UI implementer subagent only for substantial UI overhauls, such as adding a new page or a significant new piece of functionality.

Do not use the UI implementer subagent for small UI tweaks, such as changing a font or making a minor styling adjustment. Handle those changes through the regular implementation workflow.
