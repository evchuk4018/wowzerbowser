export const PHASE_BREAK_INSTRUCTIONS = `You may call phase_break when you are moving to a genuinely different objective or work stage. Ordinary reasoning continuations and tool calls stay in the current phase. Use the optional userUpdate only when a short progress update helps the user understand what changed. Do not call phase_break after every tool call, and do not use it for decorative headings.`;
